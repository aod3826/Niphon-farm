# LINE Mini App Integration Guide
## คู่มือปรับระบบ Niphon Farm ให้ทำงานกับ LINE Mini App

---

## ✅ สิ่งที่ได้แก้ไขแล้ว

### 1. Index.html
- ✅ เพิ่ม LIFF SDK
- ✅ เพิ่ม LIFF initialization code
- ✅ รองรับ LINE Login อัตโนมัติ
- ✅ แสดง LINE Profile แทน PIN Login
- ✅ รองรับการใช้งานทั้งใน LINE และนอก LINE

### 2. Sow_Index.html
- ✅ เพิ่ม LIFF SDK

### 3. Liff_Helper.gs (ไฟล์ใหม่)
- ✅ Configuration สำหรับ LIFF
- ✅ Helper functions ครบชุด
- ✅ Map LINE User เข้ากับฐานข้อมูลพนักงาน

---

## 🚀 ขั้นตอนการติดตั้ง

### 1. สร้าง LIFF App ใน LINE Developers Console

1. เข้าไปที่ https://developers.line.biz/console/
2. สร้าง **Provider** (ถ้ายังไม่มี)
3. สร้าง **Channel** ประเภท **LINE Login**
4. ไปที่แท็บ **LIFF** > คลิก **Add**
5. ตั้งค่า LIFF App:
   ```
   LIFF app name: Niphon Farm Smart Center
   Size: Full
   Endpoint URL: [URL ของ Google Apps Script ที่ Deploy แล้ว]
   Scope: profile, openid
   Bot link feature: Off (หรือ On ถ้าต้องการ)
   ```
6. คลิก **Add** แล้วคัดลอก **LIFF ID** (รูปแบบ: 2006503286-xxxxxxxx)

### 2. บันทึก LIFF ID ลงใน Script Properties

1. เปิด Google Apps Script Editor
2. ไปที่ **Project Settings** (ไอคอนฟันเฟือง)
3. เลื่อนลงมาที่ **Script Properties**
4. เพิ่มค่าดังนี้:
   ```
   Property: LIFF_ID
   Value: 2006503286-xxxxxxxx (LIFF ID ที่ได้จากข้อ 1)
   ```
5. (Optional) เพิ่ม LINE Channel credentials:
   ```
   Property: LINE_CHANNEL_ID
   Value: [Your Channel ID]

   Property: LINE_CHANNEL_SECRET
   Value: [Your Channel Secret]
   ```

### 3. แก้ไข LIFF ID ในไฟล์ Index.html

หาบรรทัดนี้:
```javascript
const LIFF_ID = '2006503286-KPxRrnJ8'; // เปลี่ยนเป็น LIFF ID จริงของคุณ
```

เปลี่ยนเป็น LIFF ID ที่ได้จากข้อ 1

### 4. Deploy Google Apps Script

1. ใน Apps Script Editor คลิก **Deploy** > **New deployment**
2. เลือก Type: **Web app**
3. ตั้งค่า:
   ```
   Description: Niphon Farm v20 (LIFF)
   Execute as: Me
   Who has access: Anyone
   ```
4. คลิก **Deploy**
5. คัดลอก **Web app URL** (ต้องมี /exec ท้าย)
6. นำ URL นี้ไปใส่ใน LIFF Endpoint URL (ข้อ 1.5)

### 5. เพิ่มคอลัมน์ "LINE ID" ในชีต HR_Employees (ถ้ายังไม่มี)

เพื่อให้ระบบเชื่อม LINE User เข้ากับพนักงาน:

1. เปิดชีต `HR_Employees`
2. เพิ่มคอลัมน์ใหม่ชื่อ **"LINE ID"** (ข้างหลัง "รูปโปรไฟล์")
3. สำหรับพนักงานที่มี LINE ให้ใส่ LINE User ID ลงไป
   (ดู LINE ID ได้จาก LIFF Console หรือให้พนักงานเปิดแอปครั้งแรกแล้วระบบจะบอก)

### 6. แก้ไข Config.gs (เพิ่มคอลัมน์ LINE)

เปิดไฟล์ `Config.gs` แล้วแก้ในส่วนนี้:

```javascript
DB: {
  HR_EMP: {
    NAME: "HR_Employees",
    COL: {
      ID: "รหัสพนักงาน",
      NAME: "ชื่อ-สกุล",
      ROLE: "ตำแหน่ง",
      PIN: "PIN",
      PASS: "รหัสผ่าน",
      STATUS: "สถานะ",
      PHONE: "เบอร์โทร",
      LINE: "LINE ID",  // <-- เพิ่มบรรทัดนี้
      ADDR: "ที่อยู่",
      BANK: "ชื่อธนาคาร",
      ACC_NO: "เลขบัญชี",
      IMG: "รูปโปรไฟล์"
    }
  },
  // ... ส่วนอื่นๆ คงเดิม
}
```

---

## 📱 การใช้งาน

### วิธีเปิดผ่าน LINE

1. สแกน QR Code ของ LIFF App (ได้จาก LINE Developers Console)
2. หรือส่งลิงก์ LIFF URL ในแชท:
   ```
   https://liff.line.me/[LIFF_ID]
   ```
3. กด "เปิด" ในข้อความ แอปจะเปิดใน LINE Browser

### ฟีเจอร์พิเศษเมื่อใช้ใน LINE

✅ **Auto Login** - ไม่ต้องใส่ PIN เพราะดึงข้อมูลจาก LINE Profile
✅ **แชร์เนื้อหา** - แชร์ข้อมูลฟาร์มให้เพื่อนได้
✅ **ส่งข้อความ** - ส่งรายงานผ่าน LINE Chat ได้
✅ **สแกน QR** - สแกน QR Code คอกหมูได้ทันที
✅ **ประสบการณ์ที่ดีกว่า** - UI/UX เหมาะกับ LINE

### การใช้งานนอก LINE (Browser ปกติ)

ถ้าเปิดผ่าน Web Browser ปกติ:
- ระบบจะ Fallback ไปใช้ **PIN Login** แบบเดิม
- ทุกฟีเจอร์ยังใช้งานได้ปกติ
- เพียงแต่ไม่มีฟีเจอร์พิเศษของ LINE

---

## 🔧 ฟีเจอร์เพิ่มเติมที่สามารถทำได้

### 1. ส่ง LINE Notify เมื่อมีเหตุการณ์สำคัญ

```javascript
// ตัวอย่างใน Module_Farm.gs
function sendLineNotify(message) {
  const config = getLIFFConfig();
  const token = config.LINE_NOTIFY_TOKEN;

  if (token) {
    UrlFetchApp.fetch('https://notify-api.line.me/api/notify', {
      method: 'post',
      headers: {
        'Authorization': 'Bearer ' + token
      },
      payload: {
        'message': message
      }
    });
  }
}

// เรียกใช้
sendLineNotify('แม่หมูหมายเลข 001 คลอดแล้ว! (10 ตัว)');
```

### 2. Rich Menu สำหรับ LINE Bot

สร้าง Rich Menu ที่มีปุ่มลัดเข้าสู่ฟีเจอร์ต่างๆ ได้ทันที

### 3. LINE Beacon สำหรับเช็คอินอัตโนมัติ

ติด LINE Beacon ที่ฟาร์ม เมื่อพนักงานเดินเข้ามาใกล้จะลงเวลาอัตโนมัติ

### 4. Flex Message สำหรับรายงาน

ส่งรายงานฟาร์มในรูปแบบสวยงามผ่าน Flex Message

---

## 🐛 แก้ปัญหาที่พบบ่อย

### 1. LIFF init failed: Invalid LIFF ID

**สาเหตุ:** LIFF ID ไม่ถูกต้อง
**วิธีแก้:** ตรวจสอบ LIFF ID ใน Index.html และ Script Properties ให้ตรงกัน

### 2. ไม่สามารถ Login ได้

**สาเหตุ:** ยังไม่ได้ Add Scope ที่ถูกต้อง
**วิธีแก้:** ไปที่ LIFF Console เช็ค Scope ต้องมี `profile` และ `openid`

### 3. แอปไม่ทำงานเมื่อเปิดใน LINE

**สาเหตุ:** Endpoint URL ไม่ถูกต้อง
**วิธีแก้:** ตรวจสอบ URL ใน LIFF Console ต้องลงท้ายด้วย `/exec`

### 4. ข้อมูลพนักงานไม่แสดง

**สาเหตุ:** ยังไม่ได้เชื่อม LINE ID กับ Employee ID
**วิธีแก้:** เพิ่มคอลัมน์ "LINE ID" ในชีต HR_Employees และกรอก LINE User ID

---

## 📝 หมายเหตุสำคัญ

1. **LIFF ID ต้องไม่เปิดเผย** - เก็บไว้ใน Script Properties
2. **Deploy ใหม่ทุกครั้งที่แก้ไข** - Apps Script ต้อง Deploy ถึงจะอัปเดต
3. **ทดสอบบน Mobile จริง** - LIFF จะทำงานได้ดีที่สุดบน Mobile LINE App
4. **สำรอง LIFF ID** - เก็บไว้ที่อื่นด้วยเผื่อลืม
5. **อ่าน LIFF Docs** - https://developers.line.biz/en/docs/liff/

---

## 🎯 Next Steps (ขั้นตอนต่อไป)

หลังจากติดตั้งเสร็จแล้ว สามารถพัฒนาต่อได้ดังนี้:

1. ✅ เพิ่ม LINE Login ในหน้า Fatten_Index.html และ Feed_Index.html
2. ✅ สร้าง LINE Bot ที่เชื่อมกับระบบ
3. ✅ เพิ่ม Rich Menu สำหรับเข้าถึงฟีเจอร์ได้เร็วขึ้น
4. ✅ ใช้ LINE Notify ส่งแจ้งเตือนเมื่อมีงานสำคัญ
5. ✅ เพิ่ม LINE Pay สำหรับระบบการเงิน (ถ้าต้องการ)

---

## 📞 ติดต่อ

หากมีปัญหาหรือข้อสงสัย สามารถ:
- อ่าน LINE Developers Docs: https://developers.line.biz/
- เข้า LINE Developers Community
- ติดต่อทีมพัฒนา Niphon Farm

---

**สร้างโดย:** AI Assistant
**อัปเดตล่าสุด:** 30 มกราคม 2026
**Version:** 1.0 (LIFF Integration)
